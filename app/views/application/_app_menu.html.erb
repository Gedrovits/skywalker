<nav class="navbar navbar-default navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <a href="#" class="navbar-brand">
        <i class="glyphicon glyphicon-fire"></i>
        I18n WYSIWYG
      </a>
    </div>
    <p class="navbar-text">
      <span class="label label-primary"><%= I18n.locale %></span>
    </p>

    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
          John Doe
          <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
          <li class="dropdown-header">Locales</li>
          <% Rails.application.config.i18n.available_locales.each do |locale| %>
            <% if locale == I18n.locale %>
              <li class="disabled"><a href="#"><%= locale %></a></li>
            <% else %>
              <li><%= link_to locale, url_for(hl: locale) %></li>
            <% end %>
          <% end %>
          <li role="separator" class="divider"></li>
          <li><a href="#sign-out">
            <i class="glyphicon glyphicon-off"></i>
            Sign out
          </a></li>
        </ul>
      </li>

      <span id="i18n-wysiwyg-changes-count" class="label label-info">0 / 0</span>
      
      <button type="button" id="i18n-wysiwyg-save" class="btn btn-success navbar-btn" disabled="disabled">
        <i class="glyphicon glyphicon-ok"></i>
        Save
      </button>
      <button type="button" id="i18n-wysiwyg-reset" class="btn btn-danger navbar-btn" disabled="disabled">
        <i class="glyphicon glyphicon-remove"></i>
        Reset
      </button>
    </ul>
  </div>
</nav>

<style>
  body { padding-top: 70px; }
</style>
